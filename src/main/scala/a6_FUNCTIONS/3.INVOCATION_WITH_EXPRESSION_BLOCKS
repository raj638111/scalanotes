
EXPLANATION
-----------------------------------------------------
	# 	Is a way to compute Input Parameter before Passing it to a Function

EXAMPLE
-----------------------------------------------------
	#50
	
	# Definition
	scala> def formatEuro(amt: Double) = f"€$amt%.2f"
	formatEuro: (amt: Double)String

	# Invocation with Expression Block
	scala> formatEuro { val rate = 1.32; 0.235 + 0.7123 + rate * 5.32 }
	res5: String = €7.97
	
